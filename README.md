# Multi-Disease-Detection
# Multi-Med Detect

Unified medical image diagnostic assistant — automatically detects image modality (Chest X-ray, Brain MRI, Retinal fundus), routes to specialized models, returns diagnosis + confidence + Grad-CAM visualizations.

## What it does
- Pneumonia detection (Chest X-ray)
- Brain tumor classification (MRI)
- Diabetic retinopathy grading (Retina)
- Auto-routing with meta-classifier
- Grad-CAM explainability
- FastAPI backend + Streamlit demo

## Quickstart (dev)
```bash
git clone <your-repo-url>
cd multi-med-detect
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
# run local demo (instructions will be added)

!!OM NAMAH SHIVAY!!